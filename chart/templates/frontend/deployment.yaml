---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "frontend.fullname" . }}
  labels:
    app: {{ template "frontend.fullname" . }}
    chart: {{ template "chart.fullname" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "frontend.fullname" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "frontend.fullname" . }}
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: flask-frontend
          image: "{{ .Values.common.dockerRegistry }}/{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
          imagePullPolicy: {{ .Values.common.imagePullPolicy }}
          name: {{ template "frontend.fullname" . }}
          ports:
            - name: 'react'
              containerPort: 3000
              protocol: TCP
      imagePullSecrets:
        - name: {{ .Values.common.imagePullSecret }}
    {{- if .Values.global.rbac.pspEnabled }}
      serviceAccountName: reanplatform
    {{- end }}
    {{- if .Values.frontend.nodeSelector }}
      nodeSelector: {{ toYaml .Values.frontend.nodeSelector | indent 8 }}
    {{- end }}